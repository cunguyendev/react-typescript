import UNIT from './unit'

/**
 * All of breakpoints is developed to be mobile first
 */
export default {
  SM: `${576}${UNIT.PIXEL}`,
  MD: `${768}${UNIT.PIXEL}`,
  LG: `${992}${UNIT.PIXEL}`,
  XL: `${1200}${UNIT.PIXEL}`,
}